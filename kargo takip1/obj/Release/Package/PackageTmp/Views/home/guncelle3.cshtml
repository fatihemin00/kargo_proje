@model kargo_takip1.Models.Sirketler
@{
    ViewBag.Title = "güncelle";
}
<h2 style="text-align:center">Ürün Düzenleme Sayfası</h2>

<div class="container">
    <div class="row">
        <div class="col-md-6">
            <form id="sirketler">
                <div class="form-group">
                    @*<label> ıd Giriniz</label>*@
                    <input hidden type="text" name="Id" class="form-control" value="@Model.Id" />
                </div>
                <div class="form-group">
                    <label> Şirket Adı Giriniz</label>
                    <input type="text" name="sirketAdi" class="form-control" value="@Model.sirketAdi" />
                </div>
                <div class="form-group">
                    <br />
                    <label> Şirket Adresi Giriniz</label>
                    <input type="text" name="sirketAdresi" class="form-control" value="@Model.sirketAdresi" />
                </div>
                <div class="form-group">
                    <label>Şirket Vergino  Giriniz</label>
                    <input type="text" name="vergiNo" class="form-control" value="@Model.vergiNo" />
                </div>
                
                <div class="form-group">
                    <label> Şİrket Vergi Dairesi Giriniz</label>
                    <input type="text" name="vergiDairesi" class="form-control" value="@Model.vergiDairesi" />
                </div>
                <div class="form-group">
                    <br />
                    <label> Şirket Telefon No Giriniz</label>
                    <input type="text" name="telefonNo" class="form-control" value="@Model.telefonNo" />
                </div>
                <div class="form-group">
                    <label> Şirketteki ilgili Kisi'sini Giriniz</label>
                    <input type="text" name="ilgiliKisi" class="form-control" value="@Model.ilgiliKisi" />
                </div>
                <br />
                <button class="btn btn-primary" type="submit"> kaydet</button>
            </form>
        </div>
    </div>
</div>
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script>
    document.addEventListener('DOMContentLoaded', function () {
        var form = document.getElementById('sirketler');

        // Formun `Enter` tuşu ile gönderilmesini engelle
        form.addEventListener('keydown', function (event) {
            if (event.key === 'Enter') {
                event.preventDefault(); // Formun gönderilmesini engeller
                var focusableElements = form.querySelectorAll('input, select, textarea'); // Form içindeki tüm giriş elemanlarını al
                var index = Array.prototype.indexOf.call(focusableElements, document.activeElement); // Mevcut odaklı elemanın indeksini al
                if (index > -1 && index < focusableElements.length - 1) {
                    focusableElements[index + 1].focus(); // Bir sonraki elemanı odakla
                } else {
                    focusableElements[0].focus(); // Son elemandaysanız, ilk elemana geri dön
                }
            }
        });

        // Formu AJAX ile gönder
        $('#sirketler').on('submit', function (e) {
            e.preventDefault(); // Formun normal şekilde gönderilmesini engelle

            var form = $(this);
            var actionUrl = form.attr('action') || '/home/guncelle3'; // Eğer formun `action` özelliği varsa kullan, yoksa varsayılan URL'yi kullan

            $.ajax({
                type: 'POST',
                url: actionUrl,
                data: form.serialize(), // Formun tüm verilerini seri hale getir
                success: function (response) {
                    // Başarılı yanıt durumunda yapılacak işlemler
                    alert('Veri başarıyla kaydedildi!');
                    // İsterseniz burada sayfayı yenileyebilir veya başka bir işlem yapabilirsiniz
                    window.location.href = '/home/sirketlerlistele'; // Formu sıfırla (isteğe bağlı)
                },
                error: function (xhr, status, error) {
                    // Hata durumunda yapılacak işlemler
                    alert('Bir hata oluştu: ' + error);
                }
            });
        });
    });
</script>
</script>

